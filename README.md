# Velocity Router

This plugin is designed for Velocity Proxy servers.

It will route players based on their game version, and the last server they were connected to.

## Setup

Place the plugin jar into Velocity's `plugins` folder and (re)start velocity. The plugin will do the rest